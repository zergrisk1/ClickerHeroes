// constants.dart

// 최대 경험치 증가 배율
const double EX_RATE = 1.2;

// 기본 체력 회복량
final BigInt HEALTH_RECOVERY = BigInt.from(10);

// 기본 마나 회복량
final BigInt MANA_RECOVERY = BigInt.from(5);

// 기본 클릭 공격력
final BigInt BASE_CLICK_ATTACK_POWER = BigInt.from(10);

// 기본 틱당 공격력
final BigInt BASE_TICK_ATTACK_POWER = BigInt.from(2);

// 최대 레벨
final BigInt MAX_LEVEL = BigInt.from(100);

// 치명타 확률 기본값
const double BASE_CRITICAL_HIT_CHANCE = 0.1; // 10%

// 치명타 공격력 배율
const double CRITICAL_HIT_DAMAGE_MULTIPLIER = 2.0;

// 초기 금화
final BigInt INITIAL_GOLD = BigInt.from(100);

// 초기 다이아몬드
final BigInt INITIAL_DIAMONDS = BigInt.from(50);

// 초기 최대 체력
final BigInt INITIAL_MAX_HEALTH = BigInt.from(100);

// 초기 최대 마나
final BigInt INITIAL_MAX_MANA = BigInt.from(50);

// 초기 경험치
final BigInt INITIAL_EXPERIENCE = BigInt.from(0);

// 초기 최대 경험치
final BigInt INITIAL_MAX_EXPERIENCE = BigInt.from(100);

// 잡몹 체력바 넓이
final double MONSTER_HEALTH_BAR_WIDTH = 100;

// 잡몹 체력바 높이
final double MONSTER_HEALTH_BAR_HEIGHT = 10;
